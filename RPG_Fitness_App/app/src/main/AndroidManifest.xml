<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.rpg_fitness_app.android.rpg_fitness_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".kingdom_Package.BuildingPopUp"
            android:theme="@style/AppTheme"
            ></activity>

        <activity android:name=".kingdom_Package.MysteryPopUp"
            android:theme="@style/AppTheme"
            ></activity>

        <activity android:name=".kingdom_Package.TilePopUp"
            android:theme="@style/AppTheme"
            ></activity>

        <activity android:name=".kingdom_Package.KingdomActivity"
            android:theme="@style/AppTheme"
            ></activity>

        <activity android:name=".character_Package.ItemPopUpHelm"
            android:theme="@style/AppTheme.popUpCharacterScreen"
            ></activity>

        <activity android:name=".character_Package.CharacterActivity"
            android:theme="@style/AppTheme"
            android:exported="true"
            ></activity>


        <activity android:name=".character_Package.characterCreationActivity"
            android:theme="@style/AppTheme"
            android:exported="true"
            ></activity>

        <activity android:name=".fitness_Package.TipMaster"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:exported="true"
            ></activity>

        <activity android:name=".fitness_Package.GoalActive"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:exported="true"
            ></activity>


    </application>

</manifest>